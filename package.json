{
    "name": "docker-weather-dashboard",
    "description": "Dockerization of weather-dashboard project",
    "version": "0.0.1",
    "dependencies": {},
    "scripts": {
        "start": "docker-compose up --build -d",
        "stop": "docker-compose down",
        "update": "git submodule update --recursive --remote"
      }
}